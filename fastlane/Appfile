app_identifier(ENV["BUNDLE_ID"] || "com.plainware.tray")
apple_id(ENV["APPLE_ID"])               # your Apple Developer account email
team_id(ENV["DEVELOPMENT_TEAM"])        # 10-char Developer Team ID
itc_team_id(ENV["ASC_TEAM_ID"]) if ENV["ASC_TEAM_ID"]
